/* eslint-disable no-undef */

describe('Suite 1', () => {
  test('Test addition', () => {
    expect(1 + 2).toBe(4);
  });

  test('Test some more addition', () => {
    expect(1 + 2).toBe(3);
  });
});
