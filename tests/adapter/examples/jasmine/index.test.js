/* eslint-disable no-undef */

describe('Suite 1', () => {
  it('Test addition', () => {
    expect(1 + 2).toBe(4);
  });

  it('Test some more addition', () => {
    expect(1 + 2).toBe(3);
  });
});
