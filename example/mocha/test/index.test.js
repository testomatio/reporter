const assert = require('assert');

describe('Sample mocha suite', () => {
  it('Sample mocha test @Tca70c8c4', () => {
    assert.equal([1, 2, 3].indexOf(4), -1);
  });
});

describe('Sample mocha suite 2', () => {
  it('Sample mocha test 2 @Tc2171bd2', () => {
    assert.equal(1, 1);
  });
});
